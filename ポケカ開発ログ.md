## 2025-12-22 完了項目
- 全カード画像一括設定（26枚中26枚設定完了）
- カード画像表示位置修正（CardUIルートImageコンポーネントに設定）
- CardImage子オブジェクト削除（Prefab構造の整理）
- カード詳細パネル画像表示機能追加
- 風パーティクルエフェクト作成完了（3つずつ段階的変更方式で12ラウンド実施）

---

## 2025-12-22 (42) - 風パーティクルエフェクト作成

**実装内容**: ユーザー様作成の「Particle System」を風エフェクトに調整（段階的変更方式）

**背景**:
- 前セッションでパーティクルが何度も見えなくなる問題が発生
- ユーザー様から「一度に3つずつ変更し、どの項目を操作したか必ず報告する」方式を指示
- この方式により、どの変更が問題を引き起こすか追跡可能にする

**実施した変更（12ラウンド・36項目）**:

### Round 1 - Main Module Basics
1. startLifetime: 5→2
2. startSpeed: 5→3
3. startSize: 1→0.3

### Round 2 - Renderer for Streaks
1. emissionRate: 10→2
2. renderMode: Billboard→Stretch
3. lengthScale: 2→12

### Round 3 - Noise Module Start
1. velocityScale: 0→0.8
2. noise.enabled: false→true
3. noise.strengthX: 0→0.05

### Round 4 - Noise Refinement
1. noise.strengthZ: 0→0.05
2. noise.frequency: 1→0.4
3. noise.scrollSpeed: 1→0.6

### Round 5 - Color and Limits
1. startColor: #FFFFFF→#E6F2FF
2. noise.damping: false→true
3. maxParticles: 1000→200

### Round 6 - Shape Changes
1. shape.shapeType: Cone→Box
2. shape.scale: default→(0.5,0.5,0.5)
3. velocityOverLifetime.enabled: false→true

### Round 7 - Velocity Direction
1. velocityOverLifetime.x: 0→2
2. velocityOverLifetime.y: 0→0
3. velocityOverLifetime.z: 0→0

### Round 8 - Lifetime Animations
1. colorOverLifetime.enabled: false→true
2. sizeOverLifetime.enabled: false→true
3. sortingOrder: 0→100

### Round 9 - Trails for Ribbon Effect
1. trails.enabled: false→true
2. trails.lifetime: 0→0.8
3. trails.ratio: 0→1.0

### Round 10 - Space and Rotation
1. simulationSpace: Local→World
2. sortingLayerName: Default→UI
3. shape.rotation: (0,0,0)→(0,225,0)

### Round 11 - Alpha Fade
1. colorOverLifetime.gradient.alphaKeys[1].time: 1.0
2. colorOverLifetime.gradient.alphaKeys[1].alpha: 0.0
3. noise.strengthY: 0→0.03

### Round 12 - Size Fade and Trails
1. sizeOverLifetime.curve.keys[1].value: 0.0
2. trails.widthOverTrail: 0.5
3. trails.dieWithParticles: false→true

**結果**:
- ✅ 全12ラウンドでパーティクルの可視性を維持
- ✅ 帯状の風エフェクトが完成（Stretch rendering + Trails）
- ✅ フェードイン/アウト、ノイズによる揺らぎを実装
- ✅ どの変更が問題を引き起こすか追跡可能な方式を確立

**Unity.md更新**:
- 「🎨 Particle System操作の絶対原則」セクション追加
- 一度に3つまで変更、変更内容の報告、重複禁止のルールを記載

**教訓**:
- パーティクル設定変更は段階的・追跡可能な方法で実施すること
- 各変更後に必ずユーザー確認を取ること
- 変更内容を明確に報告すること

✅ **実装完了・教訓記録完了**

---

## 2025-12-22 作業計画 - ポケモン個別攻撃パーティクル作成

### 対象ポケモン（5体）

**除外**: Mashimashira（マシマシラ）、LillieClefairyEX（リーリエのピッピex）- デッキ未使用のため

#### 1. Drifloon（フワンテ）
- **タイプ**: ゴースト/飛行
- **イメージ**: ふわふわ漂う紫の煙、魂のような光
- **ステータス**: ⏳ 未作成

#### 2. Ralts（ラルトス）
- **タイプ**: エスパー/フェアリー
- **イメージ**: 小さな超能力の波動、淡い紫の光
- **ステータス**: ⏳ 未作成

#### 3. Kirlia（キルリア）
- **タイプ**: エスパー/フェアリー
- **イメージ**: より強い超能力、紫とピンクの混合光
- **ステータス**: ⏳ 未作成

#### 4. GardevoirEX（サーナイトex）
- **タイプ**: エスパー/フェアリー
- **イメージ**: 強力なサイコキネシス、華やかな紫の爆発
- **ステータス**: ⏳ 未作成

#### 5. MewEX（ミュウex）
- **タイプ**: エスパー
- **イメージ**: 神秘的なピンク/青の光、虹色のオーラ
- **ステータス**: ⏳ 未作成

### 作業手順
1. 新規シーンでパーティクル作成・テスト
2. Prefab化（`Assets/_Project/Prefabs/Particles/Attack_[ポケモン名].prefab`）
3. 再生システム実装（BattleSystemから呼び出し）
4. 動作確認

### 備考
- 各パーティクルは攻撃時に相手ポケモンの位置で再生
- 再生時間は1-2秒程度
- 自動削除（Play On Awake + Stop Action: Destroy）

⏳ **作業開始待ち**

---

## 2025-12-22 (41) - カード詳細パネル画像表示機能追加

**実装内容**: CardDetailPanel.csにカード画像表示機能を追加

**背景**:
- 前回（40）でカードUI本体の画像表示を修正完了
- カード詳細パネル（ホバー時表示）にも同じScriptableObjectを参照しているが、画像表示処理が未実装

**実装箇所**:

### CardDetailPanel.cs (51-57行) - ShowCardDetail
```csharp
// カード画像を表示（panelRootのImageコンポーネントに設定）
Image panelImage = panelRoot.GetComponent<Image>();
if (panelImage != null && cardData.cardArt != null)
{
    panelImage.sprite = cardData.cardArt;
    panelImage.color = Color.white; // 画像を正しく表示するため白にリセット
}
```

**追加変更**:
- cardTypeIconフィールドを削除
- cardTypeIcon.color設定処理を全て削除（ポケモン、トレーナー、エネルギー）
- GetTypeColorメソッドを削除

**動作**:
- ✅ panelRootのImageコンポーネントに直接カード画像を設定
- ✅ CardUIと同じ方式で統一
- ✅ 大きく表示されるため詳細確認に最適
- ✅ 不要なタイプアイコンを削除

**使用MCPツール**:
- なし（コード直接編集のみ）

⏸️ **保留（表示方法の検討中）**

---

## 2025-12-22 (40) - CardImage子オブジェクト削除

**実装内容**: CardUI.prefabからCardImage子オブジェクトを削除し、関連コード・コメントを更新

**背景**:
- 前回（39）でカード画像をCardUIルートのImageコンポーネントに設定するよう修正
- CardImage子オブジェクトは機能上不要となり、空の状態で邪魔になっていた

**削除内容**:

### 1. CardUI.prefab（Resourcesフォルダ内）
**修正ファイル**:
- `Assets/_Project/Resources/Prefabs/CardUI.prefab`
- `Assets/Resources/Prefabs/CardUI.prefab`

**変更内容**:
- CardImage子オブジェクト（GameObject、RectTransform、CanvasRenderer、Image）を完全削除
- m_Childrenから`{fileID: 8025095236385081235}` / `{fileID: 6787362817969287844}`を削除
- CardName、CardHPのみ残存

**背景**: UIManager.csが`Resources.Load<GameObject>("Prefabs/CardUI")`でResourcesフォルダからプレハブを読み込んでいたため、Resourcesフォルダ外のPrefabを修正しても反映されなかった

### 2. UIManager.cs (421-425行) - SetCardBackDisplay
**変更前**:
```csharp
// 不要な子要素（CardName, CardHP, CardImage）を全て削除
if (child.name == "CardName" || child.name == "CardHP" || child.name == "CardImage")
```

**変更後**:
```csharp
// 不要な子要素（CardName, CardHP）を全て削除
if (child.name == "CardName" || child.name == "CardHP")
```

### 3. CardSelectionHandler.cs (118行) - コメント更新
**変更前**:
```csharp
btn.transition = Selectable.Transition.None; // ColorTintを無効化（CardImageの色を保持）
```

**変更後**:
```csharp
btn.transition = Selectable.Transition.None; // ColorTintを無効化（カード画像を保持）
```

**不要プレハブ削除**:
- ❌ `Assets/Resources/Prefabs/CardUI.prefab` - 削除（古いファイル）
- ❌ `Assets/_Project/Prefabs/CardUI.prefab` - 削除（Resourcesフォルダ外で未使用）
- ✅ `Assets/_Project/Resources/Prefabs/CardUI.prefab` - 残存（実際に使用）

**動作**:
- ✅ CardUI.prefab構造がシンプル化
- ✅ CardUIルートImage + CardName + CardHPのみ
- ✅ 不要なオブジェクトがなくなり、表示がすっきり
- ✅ 重複プレハブを削除し、プロジェクト構造をクリーンアップ
- ✅ ユーザー側で動作確認完了

**使用MCPツール**:
- なし（Prefab・コード直接編集のみ）

✅ **実装完了・動作確認済み**

---

## 2025-12-22 (39) - カード画像表示位置修正

**実装内容**: UIManager.csのUpdateCardVisualメソッドを修正し、カード画像をCardUIルートのImageコンポーネントに表示

**背景**:
- 前回（38）で全26枚のカード画像を設定完了
- しかし表示位置が間違っていた：「白紙のカード枠の中に潰れた画像が入ってる感じ」
- CardImage子コンポーネントではなく、CardUI(Clone)そのもののImageコンポーネントに設定すべき

**問題の根本原因**:
- UpdateCardVisualメソッドが`child.name == "CardImage"`で子コンポーネントを探索
- しかしCardUI.prefabの構造上、ルートGameObject（CardUI）自体にImageコンポーネントが存在
- 誤った場所に画像を設定していた

**実装箇所**:

### 1. UIManager.cs (528-603行) - UpdateCardVisual（CardData版）
**変更前**:
```csharp
// Find child components
Image cardImage = null;
foreach (Transform child in cardUI.transform)
{
    if (child.name == "CardImage")
        cardImage = child.GetComponent<Image>();
}
```

**変更後**:
```csharp
// Get root Image component (CardUI itself)
Image cardImage = cardUI.GetComponent<Image>();
```

### 2. UIManager.cs (608-684行) - UpdateCardVisual（PokemonInstance版）
同様の修正を適用

**動作**:
- ✅ CardUIルートのImageコンポーネントに直接画像を設定
- ✅ CardImageを探すループを削除
- ✅ コンパイルエラーなし
- ⏳ ユーザー側で動作確認予定

**使用MCPツール**:
- なし（コード修正のみ）

✅ **実装完了・確認待ち**

---

## 2025-12-22 (38) - 全カード画像一括設定

**実装内容**: CardSpriteフォルダの全画像26枚を対応するCardDataに一括設定

**背景**:
- 前回（2025-12-16）に6枚のテスト設定を完了
- 全26枚の画像がspriteMode: 1に設定済みで、UI表示可能な状態

**設定完了カード（26枚）**:

### ポケモン（7枚）
1. fuwante.jpeg → Drifloon（フワンテ）
2. kiruria.jpeg → Kirlia（キルリア）
3. mashimashira.jpg → Mashimashira（マシマシラ）
4. myuuex.jpg → MewEX（ミュウex）
5. rarutosu.jpg → Ralts（ラルトス）
6. ri-rienopippiex.jpg → LillieClefairyEX（リーリエのピッピex）
7. sa-naitoex.jpeg → GardevoirEX（サーナイトex）

### トレーナー（17枚）
1. daichinoutuwa.png → EarthenVessel（大地の器）
2. evoryu-shon.png → RareCandy（ふしぎなアメ）
3. haopa-bo-ru.png → HyperBall（ハイパーボール）
4. kaunta-kyacchi.png → CounterCatcher（カウンターキャッチャー）
5. sakebushippo.png → MewTail（ミュウのしっぽ）
6. yuukinoomamori.png → BraveryCharm（勇気のお守り）
7. bourutaun.jpg → BeachCourt（ビーチコート）
8. hakasenokenkyuu.jpg → Research（博士の研究）
9. nanjyamo.jpg → Iono（ナンジャモ）
10. pepa-.jpg → Pepper（ペパー）
11. sugouturizao.jpg → SuperRod（すごいつりざお）
12. misuteri-ga-den.jpg → Artazon（アルタゾン）
13. nakayoshipoifin.jpg → NestBall（ネストボール）
14. ri-rienokkesshin.jpg → Boss（ボスの指令）
15. shi-kurettobokkusu.jpg → LostSweeper（ロストスイーパー）
16. yorunotanka.jpg → LevelBall（レベルボール）
17. fushiginaame.jpg → （未使用・予備）

### エネルギー（2枚）
1. kihonakuenerugi-.jpg → BasicDarkness（基本悪エネルギー）
2. kohonchouenerugi-.jpg → BasicPsychic（基本超エネルギー）

**動作**:
- ✅ SetScriptableObjectFieldValueで全26枚設定成功
- ✅ 全カードがUI表示可能な状態

**使用MCPツール**:
- SetScriptableObjectFieldValue（ScriptableObjectのcardArtフィールド設定）

✅ **全カード画像設定完了**

---

## 2025-12-16 完了項目
- カード画像表示機能実装（CardData.cardArtをUIに反映）
- MCPサーバーポート番号変更（56780→56781）により、川のやつとの並行動作を実現
- Sprite Asset Import設定手法確立（spriteMode変更の安定手法）

---

## 2025-12-16 (37) - Sprite Asset Import設定手法確立

**実装内容**: SetAssetImporterSettingではspriteModeを直接変更できない問題の解決手法確立

**背景**:
- SetAssetImporterSettingで`textureType`は変更可能だが、`spriteMode`はReflectionで検出されず変更不可
- `spriteMode: 2 (Multiple)`のままではSprite読み込みが失敗
- 前セッションでdaichinoutuwa.pngで手法を発見、今回5ファイルで安定性を検証

**確立された安定手法**:

### 1. SetAssetImporterSettingでtextureTypeをSpriteに変更
```bash
SetAssetImporterSetting("Assets/_Project/Sprits/CardSprite/image.png", "textureType", "Sprite")
```
- Unity側で自動reimport → `spriteMode: 2 (Multiple)`に設定される

### 2. .metaファイルを直接編集してspriteMode: 2 → 1に変更
```
spriteMode: 2  →  spriteMode: 1
```
- Unity側で再度自動reimport → `spriteID`が生成される

### 3. Sprite読み込みテスト
```bash
SetScriptableObjectFieldValue("Assets/_Project/Resources/PTCG/Trainers/CardData.asset", "cardArt", "Assets/_Project/Sprits/CardSprite/image.png")
```

**検証結果**:
- ✅ 6ファイルで安定動作確認:
  - daichinoutuwa.png (前セッション)
  - evoryu-shon.png → RareCandy.asset
  - haopa-bo-ru.png → HyperBall.asset
  - kaunta-kyacchi.png → CounterCatcher.asset
  - sakebushippo.png → MewTail.asset
  - yuukinoomamori.png → BraveryCharm.asset
- ✅ 全ファイルでspriteID自動生成
- ✅ 全ファイルでSprite読み込み成功

**ドキュメント更新**:
- Unity.mdに「8. Sprite Asset Import設定（spriteMode変更）」セクション追加
- 手法、検証結果、注意事項を記載

✅ **手法確立・ドキュメント化完了**

---

## 2025-12-16 (36) - MCPサーバーポート番号変更

**実装内容**: 川のやつとポケカの2プロジェクトで同時にMCPサーバーを動作させるためのポート番号変更

**背景**:
- 川のやつとポケカで同一ポート（56780）を共有していたため、ログが混在
- GetCurrentConsoleLogsで川のやつのログがポケカ側に表示される問題が発生
- ScriptableObjectへの画像設定が失敗（別プロジェクトのMCPサーバーに接続していた）

**変更内容**:

### 1. ProjectSettings/UnityNaturalMCPSetting.asset (16行目)
```yaml
port: 56781  # 変更前: 56780
```

### 2. Unity.md - プロジェクト別ポート番号セクション追加
- 川のやつ (KWS2 DynamicWaterAsset): `56780`
- ポケカ (TozawaMCP): `56781`

### 3. Mneme_Palace.md - Quick Referenceセクション更新
- プロジェクト別のコマンド例を追加

### 4. CustomMCPTools.md - 注意事項更新
- ポート番号の明記

**動作**:
- ✅ 川のやつ: `http://localhost:56780/mcp/`
- ✅ ポケカ: `http://localhost:56781/mcp/`
- ✅ 2プロジェクト同時起動・並行動作可能

**使用方法**:
1. Unityエディタ再起動（MCPサーバー設定反映のため）
2. ポケカ側MCPツール呼び出し時は必ず `56781` を使用
3. 川のやつ側MCPツール呼び出し時は従来通り `56780` を使用

✅ **実装完了・ドキュメント更新完了**

---

## 2025-12-16 (35) - カード画像表示機能実装

**実装内容**: ユーザーが用意した画像をカードUIに表示する機能を追加

**背景**:
- CardData.csには既に`public Sprite cardArt;`フィールドが存在
- UIManager.csの`UpdateCardVisual`メソッドでは色のみ設定していたが、画像を反映する処理が欠落

**実装箇所**:

### 1. UIManager.cs (566-602行) - UpdateCardVisual（CardData版）
```csharp
// Set card image sprite (if cardArt is assigned)
if (cardImage != null)
{
    if (cardData.cardArt != null)
    {
        cardImage.sprite = cardData.cardArt;
        cardImage.color = Color.white; // Reset color to white to display sprite correctly
    }
    else if (cardData is PokemonCardData pkmData)
    {
        // Fallback: Set card image color based on type if no sprite is assigned
        // （タイプ別の色設定）
    }
}
```

### 2. UIManager.cs (647-683行) - UpdateCardVisual（PokemonInstance版）
```csharp
// Set card image sprite (if cardArt is assigned)
if (cardImage != null)
{
    if (pokemon.data.cardArt != null)
    {
        cardImage.sprite = pokemon.data.cardArt;
        cardImage.color = Color.white; // Reset color to white to display sprite correctly
    }
    else if (pokemon.data is PokemonCardData pkmData)
    {
        // Fallback: Set card image color based on type if no sprite is assigned
        // （タイプ別の色設定）
    }
}
```

**動作**:
- ✅ CardData.cardArtに画像が設定されている場合、カードUIに画像を表示
- ✅ 画像が未設定の場合、従来通りタイプ別の色で表示（後方互換性維持）
- ✅ 手札、バトル場、ベンチの全カードに適用

**使用方法**:
1. UnityエディタでCardData（ScriptableObject）を開く
2. Card Art フィールドに画像（Sprite）を設定
3. ゲーム実行時、自動的にカードUIに画像が反映される

✅ **実装完了・コンパイル確認済み**

---

## 2025-11-18 完了項目
- スクロールUI修正完了（ScrollModalWindowサイズ調整、ScrollContent位置調整）
- デバッグUI実装（ターン番号・マリガン回数・手札枚数表示）
- コンソールログ出力追加（ゲーム開始時・ターン開始時の手札枚数）
- 手札枚数の正常性確認（マリガンボーナスドロー、バトル場配置による手札減少が正常に動作）

---

## 2025-11-18 (34) - デバッグUI実装（ターン番号・マリガン回数表示）

**実装内容**: 手札枚数異常調査のためのデバッグUI追加 + コンソールログ出力

**実装箇所**:

### 1. UIManager.cs (261-270行)
```csharp
// Update debug UI
if (turnText != null)
{
    turnText.text = "Turn: " + GameManager.Instance.turnCount;
}

if (mulliganLogText != null)
{
    mulliganLogText.text = "P1: Mulligan " + player1.mulligansGiven + ", Hand " + player1.hand.Count + " | P2: Mulligan " + player2.mulligansGiven + ", Hand " + player2.hand.Count;
}
```

**表示内容**:
- **TurnText**: 現在のターン番号を表示（例：`Turn: 1`）
- **MulliganLogText**: 各プレイヤーのマリガン回数と手札枚数を表示（例：`P1: Mulligan 0, Hand 7 | P2: Mulligan 1, Hand 7`）

### 2. GameManager.cs (80行) - ゲーム開始時ログ
```csharp
// Debug log: Initial hand count
Debug.Log("[GAME START] P1: Mulligan " + player1.mulligansGiven + ", Hand " + player1.hand.Count + " | P2: Mulligan " + player2.mulligansGiven + ", Hand " + player2.hand.Count);
```

### 3. GameManager.cs (176行) - ターン開始時ログ
```csharp
// Debug log: Turn start hand count
Debug.Log("[TURN " + turnCount + " START] P1: Hand " + player1.hand.Count + " | P2: Hand " + player2.hand.Count + " | Current: " + current.playerName);
```

**動作**:
- ✅ UIManager.UpdateUI()が呼ばれる度に自動更新
- ✅ ターン進行、カードプレイ等で常に最新情報を表示
- ✅ マリガン回数はPlayerController.mulligansGivenから取得（既存実装）
- ✅ ゲーム開始時に初期手札枚数をコンソールログに出力
- ✅ ターン開始時に手札枚数をコンソールログに出力

**目的**:
- 手札枚数異常（Player1=9枚、Player2=5枚）の原因調査
- ターン番号とマリガン回数の相関確認
- ゲーム進行状態の可視化

✅ **実装完了・コンパイル確認済み**

---